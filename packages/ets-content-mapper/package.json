{
  "name": "ets-content-mapper",
  "version": "0.0.1",
  "private": true,
  "description": "Content mapper that compiles .ets (Effect TypeScript) files to TypeScript",
  "tsContentMapper": {
    "exec": ["./bin/ets-mapper"]
  },
  "scripts": {
    "build": "cd ../../mapper && go build -o ../packages/ets-content-mapper/bin/ets-mapper ./cmd/ets-mapper",
    "test": "cd ../../mapper && go test ./..."
  }
}
